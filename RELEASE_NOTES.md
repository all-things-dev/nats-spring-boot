# Release Notes

## 0.1.0

### Features

1. `nats-spring-boot-starter` module enables NATS configuration using `AutoConfiguration` mechanism of Spring Boot.
2. Added basic support for `@NatsListner`.
